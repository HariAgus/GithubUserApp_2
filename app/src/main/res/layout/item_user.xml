<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimaryDark">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/circleImgUser"
            android:layout_width="95dp"
            android:layout_height="75dp"
            android:layout_marginStart="@dimen/dividerHeight"
            android:scaleType="centerCrop"
            android:src="@color/colorAccent"
            android:layout_margin="@dimen/dividerHeight"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dividerHeight"
            android:fontFamily="@font/nunito_sans_semibold"
            android:text="@string/user_name"
            android:textColor="@color/textWhitePrimary"
            android:textSize="@dimen/text_size_header"
            app:layout_constraintBottom_toBottomOf="@id/circleImgUser"
            app:layout_constraintStart_toEndOf="@id/circleImgUser"
            app:layout_constraintTop_toTopOf="@id/circleImgUser" />


    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>